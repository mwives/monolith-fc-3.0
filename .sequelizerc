const path = require('path')

module.exports = {
  config: path.resolve('src', 'infra', 'db', 'config', 'database.json'),
  'migrations-path': path.resolve('src', 'infra', 'db', 'migrations'),
}
